version: 2.1

orbs:
  windows: circleci/windows@2.0


jobs:
  build:
    description: Setup and run application tests
    executor:
      name: windows/default
    steps:
      - checkout:
          path: ~/repo
          

      - run:
          name: Install Visual Studio tools
          command: |
            choco install visualstudio2022buildtools --version=117.6.2.0 -y || exit 0
            choco install microsoft-build-tools -y || exit 0
            choco install msbuild -y || exit 0
            choco install visualstudio2019-workload-visualstudioextensionbuildtools -y || exit 0
        
workflows:
  build:
    jobs:
      - build

      